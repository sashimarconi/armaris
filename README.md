# armaris

Projeto estático — arquivos HTML/CSS/JS prontos para deploy.

Deploy automático via Vercel a partir da branch `main`.

Para forçar novo deploy: faça qualquer commit na branch `main`.
